using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AddRect : MonoBehaviour {
    //Rectangle rect = new Rectangle();
    //public float x, y, width, height;
    //private void Start()
    //{
    //    //x = transform.position.x;
    //    //y = transform.position.z;
    //    //width = transform.lossyScale.x;
    //    //height = transform.lossyScale.z;
    //    //rect = new global::Rectangle(transform.position.x,transform.position.z,transform.lossyScale.x,transform.lossyScale.z);
    //    //rect.tr = this.transform;
    //    //QuadTreeTest.instance.allRects.Add(rect);
    //}

    //public void ChangeColor()
    //{

    //    GetComponent<MeshRenderer>().material.color = new Color(1, 0, 0);
        
    //}

    //private void Update()
    //{
    //    if (transform.position.x != x)
    //    {
    //        rect.x = transform.position.x;
    //    }

    //    if (transform.position.z != y)
    //    {
    //        rect.y = transform.position.z;
    //    }

    //    if (transform.localScale.x != width)
    //    {
    //        rect.width = transform.localScale.x;
    //    }

    //    if (transform.localScale.z != height)
    //    {
    //        rect.height = transform.localScale.z;
    //    }
    //}
}
